# Python / uv build + test artifacts (uv.lock IS tracked for reproducible CI).
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Local raw-message archive scratch (CLI default; content-addressed, not committed).
.edge-archive/
