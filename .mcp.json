{
  "mcpServers": {
    "openelis-db": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "postgres-mcp==0.3.0",
        "--access-mode=restricted"
      ],
      "env": {
        "DATABASE_URI": "${OPENELIS_DB_URL}"
      }
    }
  }
}
